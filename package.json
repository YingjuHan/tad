{
  "name": "tad",
  "version": "0.14.0",
  "description": "Lerna monorepo for Tabular data viewer libraries and apps",
  "devDependencies": {
    "@types/minimatch": "3.0.5",
    "@types/jest": "^27.4.0",
    "jest": "^27.4.6",
    "lerna": "^4.0.0",
    "rimraf": "^5.0.5",
    "run-script-os": "^1.1.6",
    "ts-jest": "^27.1.2",
    "typescript": "^4.8.3"
  },
  "overrides": {
    "@types/minimatch": "3.0.5"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist --force-local --ignore-prepublish -- --legacy-peer-deps"
  }
}
